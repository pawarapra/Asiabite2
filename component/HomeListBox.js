import { StyleSheet, View, Image } from 'react-native';
import { Text } from '@rneui/themed';

export default function HomeListBox(){
    <View>
        <Text style={myListItemStyles.itemText}>{itemData.title}</Text>
    </View>
}